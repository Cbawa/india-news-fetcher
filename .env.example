# Get a free API key at https://newsdata.io/register
# The Vite dev server reads this value and injects it server-side as the
# `apikey` query parameter, so your key never ends up in the browser bundle.
NEWSDATA_API_KEY=your_api_key_here
